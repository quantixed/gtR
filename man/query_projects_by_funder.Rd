% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_projects_by_funder.R
\name{query_projects_by_funder}
\alias{query_projects_by_funder}
\title{Query projects by funder string.}
\usage{
query_projects_by_funder(funder, size = 100, page_nums, df_only = T)
}
\arguments{
\item{funder}{Funder string to search for in the funds endpoint (for example, "BBSRC").}

\item{size}{The number of results to return per page (min 10, max 100).}

\item{page_nums}{Optional page numbers of project results to scan. If omitted,
all project pages are scanned.}

\item{df_only}{Choose whether you only want a dataframe of matched projects (T)
or a list containing matched projects and metadata (F).}
}
\value{
A dataframe of projects linked to funds matching the supplied funder
string, or a list with projects plus metadata.
}
\description{
Finds funds matching a funder search string, then scans projects and returns
those that are linked to any matched fund IDs.
}
\details{
For large result sets, consider using \code{query_projects_by_funder_chunked()}
to process project pages in chunks with progress messages.
}
