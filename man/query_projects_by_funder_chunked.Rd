% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_projects_by_funder_chunked.R
\name{query_projects_by_funder_chunked}
\alias{query_projects_by_funder_chunked}
\title{Query projects by funder string in chunks.}
\usage{
query_projects_by_funder_chunked(
  funder,
  size = 100,
  page_nums,
  chunk_size = 25,
  show_progress = TRUE,
  request_pause = 1,
  max_page_retries = 6,
  skip_failed_pages = TRUE,
  df_only = T
)
}
\arguments{
\item{funder}{Funder string to search for in the funds endpoint (for example, "BBSRC").}

\item{size}{The number of results to return per page (min 10, max 100).}

\item{page_nums}{Optional page numbers of project results to scan. If omitted,
all project pages are scanned.}

\item{chunk_size}{Number of pages to process per chunk (must be a positive integer).}

\item{show_progress}{Logical indicating whether chunk progress messages should be shown.}

\item{request_pause}{Base pause in seconds between page requests.}

\item{max_page_retries}{Maximum retry attempts for a single throttled page request.}

\item{skip_failed_pages}{Logical indicating whether pages that keep returning
server errors after retries should be skipped.}

\item{df_only}{Choose whether you only want a dataframe of matched projects (T)
or a list containing matched projects and metadata (F).}
}
\value{
A dataframe of projects linked to funds matching the supplied funder
string, or a list with projects plus metadata.
}
\description{
Convenience wrapper around \code{query_projects_by_funder()} for large pulls.
It resolves matching project IDs once, then scans project pages in chunks and
reports progress to reduce the chance of long silent runs.
}
